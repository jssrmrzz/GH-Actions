name: GHActions

on: push

jobs:
  macos:
    runs-on: macos-latest
    steps:
    - run: date
  windows:
    runs-on: windows-latest
    steps:
    - run: date
  ubuntu:
    run-ons: ubuntu-latest
    steps:
    -run: date
  dependency:
    needs: [macos, windows, ubuntu]
    runs-on: macos-latest
    steps:
    - run: date
